<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Biến thể sản phẩm giờ dùng thuộc tính (attributes) + variant_attribute_values.
 * Không tạo dữ liệu mẫu; thêm biến thể thủ công trong Admin > Sản phẩm > Sửa > Biến thể.
 */
class ProductVariantSeeder extends Seeder
{
    public function run(): void
    {
        // No sample data; add variants manually in admin.
    }
}
